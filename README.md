# Object-Detection-Project
Built a custom object detection model using Tensorflow 2 Object Detection API.
Used models such as Fast R-CNN and YOLO(You Only Look Once). 
Dataset used has more than 1800 image files belonging to different categories of objects.
